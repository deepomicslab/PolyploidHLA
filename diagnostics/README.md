# Diagnostics and Offline Experiments

This directory contains scripts that are not part of the default `polyphase_v2.sh` production path: validation runners, offline scorers, diagnostic harnesses, and historical prototypes kept for reference.

The production entry points remain in the parent `scripts/` directory so existing runs can continue to call them without path changes.
